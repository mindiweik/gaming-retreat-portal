{
  "name": "gaming-retreat-portal",
  "version": "0.0.0",
  "private": true,
  "description": "Gaming retreat portal — lottery module (Phase 2 critical path)",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "test:lottery": "bun test tests/lottery.test.ts",
    "sim": "bun run src/lottery/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/bun": "^1.3.0"
  }
}
